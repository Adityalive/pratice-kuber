apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: product-ingress
  labels:
    app.kubernetes.io/name: product-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: product-service
                port:
                  number: 80
